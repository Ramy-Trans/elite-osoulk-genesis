[build]
  command   = "npm run build"
  publish   = "dist/client"

[build.environment]
  NODE_VERSION  = "20"
  VITE_API_BASE = ""

# Proxy /api/* to the Replit backend server-side — same origin, no CORS.
[[redirects]]
  from   = "/api/*"
  to     = "https://f4e8c49d-5d4d-4118-87bf-85bc53763c88-00-1rjy0spubae9m.riker.replit.dev/api/:splat"
  status = 200
  force  = true

# SPA fallback — every client-side route serves index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options        = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection       = "1; mode=block"
    Referrer-Policy        = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
